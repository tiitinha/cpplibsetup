#ifndef STRSPLIT_H
#define STRSPLIT_H

#include <vector>
#include <sstream>
#include <string>
#include <iterator>

std::vector<std::string> split(const std::string &s, const std::string &delimiter);
#endif
